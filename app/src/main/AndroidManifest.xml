<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppDoctorNow"
        tools:targetApi="31">
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad de Login Administrador Recepcionista -->
        <activity
            android:name=".ui.activity.LoginAdminActivity"
            android:label="Iniciar Sesión" />

        <!-- Actividad de Login -->
        <activity
            android:name=".ui.activity.LoginActivity"
            android:label="Iniciar Sesión" />

        <!-- Actividad de Registro -->
        <activity
            android:name=".ui.activity.RegistroActivity"
            android:label="Crear Cuenta" />

        <!-- Actividad de Dashboard para Pacientes -->
        <activity
            android:name=".ui.activity.DashboardPacienteActivity"
            android:label="Dashboard Paciente" />

        <!-- Actividad de Dashboard para Administradores/Recepcionistas -->
        <activity
            android:name=".ui.activity.DashboardAdminActivity"
            android:label="Dashboard Administrador" />

    </application>

</manifest>